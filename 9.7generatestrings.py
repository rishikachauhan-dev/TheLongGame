'''
1st function-subset recursion- inside put both s1 and s2-inside the blue print
pick and not pick when its already in result for each s1 and s2--filter
count picks in both s1 and s2
compare
return condition

filter-
if in result? skip else append
'''
